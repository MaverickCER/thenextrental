import { createTheme } from '@mui/material';

const theme = createTheme(undefined, {
  palette: {
    contact: {
      accent: '#A58100',
      background: '#FCFCFC',
      color: '#171717',
    },
    home: {
      accent: '#A58100',
      background: '#FCFCFC',
      color: '#171717',
    },
    properties: {
      accent: '#A58100',
      background: '#FCFCFC',
      color: '#171717',
    },
  },
});

export default theme;
